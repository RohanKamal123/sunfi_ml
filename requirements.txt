numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
imbalanced-learn>=0.11
xgboost>=2.0
shap>=0.44
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1
joblib>=1.3
jupyter>=1.0

# Report generation (python -m src.report)
reportlab>=4.0
pillow>=10.0
pypdf>=4.0

# Optional. The pipeline runs without them: CatBoost is dropped from the model
# zoo and the AutoML benchmark reports itself unavailable.
catboost>=1.2      # Paper 3's best model, so its claim is tested not quoted
flaml>=2.1         # AutoML headroom benchmark

